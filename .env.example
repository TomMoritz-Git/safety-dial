# Copy to .env and fill in. The package loads this via config.load_env().
# Anthropic key for the LLM judge (claude-haiku-4-5).
ANTHROPIC_API_KEY=
# Hugging Face token with access to gated repos (Gemma-3, Llama-3.2).
HF_TOKEN=
